name: form_builder_number_text_field
description: "A specialized FormBuilder textfield for numeric input, featuring automatic data type casting, strict input formatting, and localized decimal separators."
version: 0.9.0
resolution: workspace

homepage: https://o7planning.github.io
repository: https://github.com/o7planning/form_builder_number_text_field
example: example/main.dart

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  number_text_input_formatter: ^1.0.0+9
  flutter_form_builder: ^10.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
